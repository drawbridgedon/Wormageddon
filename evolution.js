/**
 * Evolution mode — in development.
 *
 * Planned:
 *   - Worms reproduce when they reach a size threshold
 *   - Offspring inherit parent traits with small random mutations
 *   - Population graphs track species counts over time
 *   - Natural selection pressures (food scarcity, predation)
 */

export function initEvolution() {
  // placeholder — nothing to initialise yet
}
